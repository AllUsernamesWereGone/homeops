# Firmware

This directory contains firmware projects for ESP32 and Raspberry Pi Pico based devices.

Firmware projects communicate with the HomeOps platform through MQTT.

The MQTT topic and payload contract is documented in `docs/mqtt-contract.md`.
